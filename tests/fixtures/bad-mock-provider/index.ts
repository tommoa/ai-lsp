/**
 * Bad mock provider that doesn't conform to @ai-sdk package format.
 *
 * This package is intentionally malformed - it doesn't export any function
 * starting with "create" as required by the @ai-sdk pattern.
 *
 * Used for testing error handling when loading invalid provider packages.
 */
